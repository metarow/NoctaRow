# Obsidian – gerätespezifischer Zustand, niemals synchronisieren
docs/.obsidian/

# Papierkorb
docs/.trash/

# Signierschluessel — duerfen das Repo nie verlassen (siehe docs/04)
cosign.key
*.pem
*.key

# Build-Artefakte des bootc-image-builder (siehe `noctarow disk`)
output/

# Betriebssystem
.DS_Store
Thumbs.db
desktop.ini

# Optional: große Binärdateien ausschließen, wenn Webspace knapp ist
# *.mp4
# *.zip
