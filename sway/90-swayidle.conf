# Noctarow: verdraengt /usr/share/sway/config.d/90-swayidle.conf.
#
# PRUEFEN: ob Noctalia v5 Idle/Lock selbst uebernimmt. Falls nicht, hier die
# eigene swayidle-Zeile eintragen -- der Basename bleibt derselbe, dieser
# Slot wird nur geerbt.
